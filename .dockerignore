# Never bake secrets or local state into an image.
.env
.env.*
!.env.example
# Load-bearing: `next build` traces the default SQLITE_PATH and would copy a
# developer's database (phone numbers included) into .next/standalone/data.
data
coverage
internal-thoughts

node_modules
.next
out
build
*.tsbuildinfo
next-env.d.ts

.git
.gitignore
.DS_Store
tests
README.md
