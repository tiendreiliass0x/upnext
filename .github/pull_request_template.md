## What

<!-- One or two sentences: what changes for a DJ or a guest. -->

## Why

<!-- The problem, or the request, that made this worth doing. -->

## Trade-offs

<!-- What this deliberately does not do, and what it costs. "None" is a fine
     answer only when it is true. -->

## Checklist

- [ ] `bun run test:coverage`, `bunx tsc --noEmit`, `bun run lint` pass locally
- [ ] Schema changes migrate an existing database (see `src/lib/db.ts`)
- [ ] README updated if behaviour or configuration changed
